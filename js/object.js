// オブジェクトの宣言と値の代入を行う
const personalData = { name: '侍太郎', age: 36, gender: '男性'};
// オブジェクトの値を更新する
console.log(personalData);

// 'age'というキーの値を更新する
personalData.age = 37;

// 新しくプロパティ（キーと値）を追加する
personalData.addres = '東京都';

// オブジェクトの値を出力する
console.log(personalData);

// 男性を追加
console.log(personalData.gender);